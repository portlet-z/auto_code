package ${packageName}.modules.${moduleName}.web;

import cn.people.one.modules.${moduleName}.model.${ClassName};
import cn.people.one.modules.${moduleName}.service.impl.${ClassName}Service;
import lombok.extern.slf4j.Slf4j;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

import java.util.List;

/**
* ${functionName}Controller
* @author ${author}
*/
@RestController
@RequestMapping("/${adminPath}/${moduleName}/")
@Slf4j
public class ${ClassName}Controller {

    @Autowired
    private ${ClassName}Service ${className}Service;

    @RequestMapping("${controllerPath}/")
    public List<${ClassName}> getList(){
        return null;
    }

    @RequestMapping("/pages")
    public List<${ClassName}> getPages(){
        return null;
    }

    @RequestMapping("/insert")
    public ${ClassName} insert(${ClassName} ${className}){
        return ${className}Service.insert(${className});
    }

    @RequestMapping("/delete/{id}")
    public int delete(@PathVariable Integer id){
        return ${className}Service.delete(id);
    }

    @RequestMapping("/${controllerPath}/{id}")
    public int getDetailById(@PathVariable Integer id){
        return ${className}Service.fetch(id);
    }

    @RequestMapping("/update")
    public int update(${ClassName} ${className}){
        return ${className}Service.update(${className});
    }
}